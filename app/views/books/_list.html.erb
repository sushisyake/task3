<div class="col-md-3" >
  <h1 class =  "my-3">User info</h1>
  <table class = "table table-hover table-inverse">
    <thead>
      <tr><th><%= image_tag user.get_profile_image(100, 100) %></th>
      <tr><th>name  <%=  user.name %></th></tr>
      <tr><th>introduction  <%= user.introduction%></th></tr>
    </thead>
  </table>
  <a class = "col-10">
    <%= link_to edit_user_path(user.id), class:"btn btn-outline-dark btn-sm btn-block mb-3 text-center"  do%>
       <i class="fas fa-user-cog" style="color: black;"></i>
    <% end %>
  </a>
<h1>New book</h1>

<% if book.errors.any? %>
  <%= book.errors.count %>errors prohibited this obj from being saved:
  <ul>
    <% book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

  <%= form_with model: book do |f| %>
    <h4>Title</h4>
      <%= f.text_field :title %>
    <h4>Opinion</h4>
      <%= f.text_area :body %>
    <button class="btn btn-success" type="submit">Create Book</button>
  <% end %>
  </div>