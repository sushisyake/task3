<% @books.each do |book| %>
<div>
  <%= link_to book_path(book.id) do %>
    <%= image_tag 'sample-author1.jpg' %>
    <%= book.user.name %>
  <% end %>
  <p>ショップ名：<%= book.title %></p>
  <p>説明：<%= book.body %></p>
</div>
<% end %>